{
  "name": "concords",
  "version": "0.0.0",
  "private": "true",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "docs:dev": "yarn --cwd packages/site docs:dev",
    "docs:build": "yarn --cwd packages/site docs:build",
    "playground:dev": "yarn --cwd packages/playground dev",
    "playground:build": "yarn --cwd packages/playground build",
    "identity:dev": "yarn --cwd packages/identity dev",
    "identity:build": "yarn --cwd packages/identity build"
  },
  "dependencies": {
    "@fontsource/rajdhani": "^4.1.0",
    "vue": "^3.0.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^1.0.4",
    "@vue/compiler-sfc": "^3.0.5",
    "autoprefixer": "^10.2.1",
    "eslint": "^7.17.0",
    "eslint-plugin-vue": "^7.4.1",
    "postcss": "^8.2.4",
    "vite": "^2.0.0-beta.12"
  }
}
