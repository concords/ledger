{
  "name": "concords",
  "version": "0.0.0",
  "license": "GPL-3.0-or-later",
  "private": "true",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:core": "yarn --cwd packages/core tsc -p tsconfig.json",
    "build:identity": "yarn --cwd packages/identity tsc -p tsconfig.json",
    "build:ledger": "yarn --cwd packages/ledger tsc -p tsconfig.json",
    "build:plugins": "yarn --cwd packages/plugins tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^1.0.4",
    "autoprefixer": "^10.2.1",
    "eslint": "^7.17.0",
    "eslint-plugin-vue": "^7.4.1",
    "postcss": "^8.2.4",
    "tslib": "^2.1.0",
    "typescript": "^4.2.3",
    "vite": "^2.0.0-beta.12"
  }
}
