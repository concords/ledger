{
  "name": "@concords/identity",
  "version": "1.0.5",
  "description": "ECDSA key-pair  utilities",
  "repository": "https://github.com/concords/core/packages/identity",
  "author": "dev@concords.app",
  "license": "GPL-3.0-or-later",
  "main": "dist/build/concords-identity.cjs.js",
  "module": "dist/build/concords-identity.esm.js",
  "types": "types",
  "files": [
    "/dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && rollup -c --environment INCLUDE_DEPS,BUILD:production",
    "pre-publish": "yarn build",
    "publish": "yarn npm publish --access public"
  }
}
